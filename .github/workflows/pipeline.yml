name: Simply Explained Pipeline

on:
  schedule:
    - cron: "0 14 * * 3"
  workflow_dispatch:
    inputs:
      topic_slug:
        description: "Optional slug from config/topic_queue.json (e.g. data-structures — not the full title)"
        required: false
        type: string
      movie_slug:
        description: "Legacy alias for topic_slug (slug only, e.g. data-structures)"
        required: false
        type: string
      skip_phase1:
        description: "Skip Phase 1; restore script from a prior run's phase1-output artifact"
        required: false
        type: boolean
        default: false
      skip_phase2:
        description: "Skip Phase 1+2; restore narration from a prior run's failed-output artifact"
        required: false
        type: boolean
        default: false
      source_run_id:
        description: "GitHub Actions run ID with phase1-output or failed-output artifact"
        required: false
        type: string

env:
  VPS_URL: ${{ secrets.VPS_WEBHOOK_URL }}
  VPS_SECRET: ${{ secrets.VPS_WEBHOOK_SECRET }}

jobs:
  script-and-audio:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: write
      actions: read
    outputs:
      run_id: ${{ steps.meta.outputs.run_id }}
    steps:
      - uses: actions/checkout@v4

      - name: Restore topic history from latest artifact
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          ARTIFACT_ID=$(gh api "repos/${{ github.repository }}/actions/artifacts?name=topic-history&per_page=5" \
            --jq '[.artifacts[] | select(.expired==false)][0].id // empty')
          if [ -z "${ARTIFACT_ID}" ]; then
            echo "No prior topic-history artifact"
            exit 0
          fi
          gh api "repos/${{ github.repository }}/actions/artifacts/${ARTIFACT_ID}/zip" > /tmp/topic-history.zip
          unzip -o /tmp/topic-history.zip -d /tmp/topic-history-out
          if [ -f /tmp/topic-history-out/topic_history.json ]; then
            cp /tmp/topic-history-out/topic_history.json config/topic_history.json
          elif [ -f /tmp/topic-history-out/config/topic_history.json ]; then
            cp /tmp/topic-history-out/config/topic_history.json config/topic_history.json
          fi

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install --pre -r requirements.txt

      - name: SECE upstream contract regression tests
        env:
          PYTHONPATH: src
        run: |
          pip install -q pytest
          python -m pytest tests/test_upstream_contract.py tests/test_sece.py -q

      - name: Install ffmpeg (ffmpeg + ffprobe for Phase 2 audio)
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Restore failed-output from prior run
        if: ${{ github.event.inputs.skip_phase2 == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: failed-output
          path: output
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.inputs.source_run_id }}

      - name: Restore phase1-output from prior run
        if: ${{ github.event.inputs.skip_phase1 == 'true' && github.event.inputs.skip_phase2 != 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: phase1-output
          path: output
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.inputs.source_run_id }}

      - name: Validate failed-output restore
        if: ${{ github.event.inputs.skip_phase2 == 'true' }}
        run: |
          required=(
            output/script.txt output/scene_clips.json output/metadata.json
            output/narration.mp3 output/scene_durations.json output/captions.srt
          )
          for f in "${required[@]}"; do
            test -s "$f" || { echo "Missing from failed-output: $f"; exit 1; }
          done

      - name: Validate phase1-output restore
        if: ${{ github.event.inputs.skip_phase1 == 'true' && github.event.inputs.skip_phase2 != 'true' }}
        run: |
          required=(
            output/script.txt output/scene_clips.json output/metadata.json
            output/scenes.json output/youtube_seo.json
          )
          for f in "${required[@]}"; do
            test -s "$f" || { echo "Missing from phase1-output: $f"; exit 1; }
          done

      - name: Verify VPS reachable
        run: |
          python - <<'PY'
          import os, socket, sys, time
          from urllib.parse import urlparse

          import httpx

          base = os.environ.get("VPS_URL", "").rstrip("/")
          if not base:
              sys.exit("VPS_URL secret is empty")

          parsed = urlparse(base)
          host = parsed.hostname
          port = parsed.port or (443 if parsed.scheme == "https" else 80)

          def port_open() -> bool:
              try:
                  with socket.create_connection((host, port), timeout=15):
                      return True
              except OSError:
                  return False

          last_err = None
          for attempt in range(5):
              try:
                  r = httpx.get(f"{base}/health", timeout=httpx.Timeout(15.0, connect=15.0, read=15.0))
                  r.raise_for_status()
                  body = r.json()
                  print(body)
                  if body.get("status") == "busy":
                      print("VPS worker is busy rendering; continuing pipeline.")
                  break
              except httpx.ReadTimeout as exc:
                  if port_open():
                      print(
                          {
                              "status": "busy",
                              "service": "retro-clip-worker",
                              "note": "health timed out but port is open — worker likely rendering on 1GB VPS",
                          }
                      )
                      break
                  last_err = exc
              except Exception as exc:
                  last_err = exc

              if attempt + 1 < 5:
                  wait = 15 * (attempt + 1)
                  print(f"VPS health retry {attempt + 2}/5 in {wait}s: {last_err}")
                  time.sleep(wait)
                  continue

              print(f"VPS health check failed for {base}/health: {last_err}")
              print("Worker may be busy rendering — retries exhausted.")
              print("If SSH localhost works, run: sudo bash /opt/retro-movies/scripts/vps-open-firewall-port.sh 8766")
              raise last_err
          PY

      - name: Verify NotebookLM auth
        if: ${{ github.event.inputs.skip_phase1 != 'true' && github.event.inputs.skip_phase2 != 'true' }}
        env:
          NOTEBOOKLM_AUTH_JSON: ${{ secrets.NOTEBOOKLM_AUTH_JSON }}
          NOTEBOOKLM_PROFILE: study
        run: |
          if [ -z "$NOTEBOOKLM_AUTH_JSON" ]; then
            echo "NOTEBOOKLM_AUTH_JSON secret is empty"
            exit 1
          fi
          notebooklm auth check --test --json

      - name: Phase 1 — script + scene map
        if: ${{ github.event.inputs.skip_phase1 != 'true' && github.event.inputs.skip_phase2 != 'true' }}
        env:
          NOTEBOOKLM_AUTH_JSON: ${{ secrets.NOTEBOOKLM_AUTH_JSON }}
          NOTEBOOKLM_PROFILE: study
        run: |
          ARGS=(--output output)
          TOPIC_SLUG="${{ github.event.inputs.topic_slug }}"
          if [ -z "$TOPIC_SLUG" ]; then
            TOPIC_SLUG="${{ github.event.inputs.movie_slug }}"
          fi
          if [ -n "$TOPIC_SLUG" ]; then
            ARGS+=(--topic-slug "$TOPIC_SLUG")
          fi
          python src/phase1_script.py "${ARGS[@]}"

      - name: Build thumbnail (dark bg + text overlay)
        if: ${{ github.event.inputs.skip_phase2 != 'true' }}
        run: python src/build_thumbnail.py --input output
        continue-on-error: true

      - name: Capture run_id
        id: meta
        run: |
          RUN_ID=$(python -c "import json; print(json.load(open('output/metadata.json'))['run_id'])")
          echo "run_id=$RUN_ID" >> "$GITHUB_OUTPUT"

      - uses: actions/upload-artifact@v4
        if: ${{ github.event.inputs.skip_phase2 != 'true' }}
        with:
          name: phase1-output
          path: output/
          if-no-files-found: error

      - name: Phase 2 — Azure TTS (+ edge fallback)
        if: ${{ github.event.inputs.skip_phase2 != 'true' }}
        env:
          AZURE_SPEECH_KEY: ${{ secrets.AZURE_SPEECH_KEY }}
          AZURE_SPEECH_REGION: ${{ secrets.AZURE_SPEECH_REGION }}
        run: python src/phase2_audio.py --input output --output output --require-azure

      - name: SECE compile (beats + render_ir)
        if: ${{ github.event.inputs.skip_phase2 != 'true' }}
        run: python src/compile_composition.py --input output

      - name: Trigger VPS clip render
        run: python src/trigger_vps.py --input output --run-id "${{ steps.meta.outputs.run_id }}"

      - name: Verify pipeline outputs
        run: |
          required=(
            output/script.txt output/scene_clips.json output/metadata.json
            output/narration.mp3 output/scene_durations.json output/captions.srt
            output/youtube_seo.json output/thumbnail.json
          )
          for f in "${required[@]}"; do
            test -s "$f" || { echo "Missing or empty: $f"; exit 1; }
          done

      - uses: actions/upload-artifact@v4
        with:
          name: mid-pipeline
          path: output/
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        with:
          name: topic-history
          path: config/topic_history.json
          if-no-files-found: warn

      - name: Upload partial output on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: failed-output
          path: output/
          if-no-files-found: ignore

      - name: Persist topic history to main
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add config/topic_history.json
          if git diff --staged --quiet; then exit 0; fi
          git commit -m "chore: update topic history after ${{ steps.meta.outputs.run_id }}"
          git pull --rebase origin HEAD || true
          git push origin HEAD:${{ github.ref_name }}

  wait-for-render:
    needs: script-and-audio
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: mid-pipeline
          path: output

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install ffmpeg (ffprobe for render validation)
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - run: pip install httpx

      - name: Poll VPS until render complete
        run: python src/poll_vps.py --run-id "${{ needs.script-and-audio.outputs.run_id }}" --timeout 7200

      - name: Download final video from VPS
        run: python src/download_vps_output.py --run-id "${{ needs.script-and-audio.outputs.run_id }}" --output output

      - name: Validate render duration
        run: python src/validate_render.py --input output

      - uses: actions/upload-artifact@v4
        with:
          name: final-video
          path: |
            output/final_video.mp4
            output/thumbnail.png
            output/captions.srt
            output/metadata.json
            output/youtube_seo.json
            output/script.txt
          if-no-files-found: error

  upload:
    needs: [script-and-audio, wait-for-render]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - uses: actions/download-artifact@v4
        with:
          name: final-video
          path: output

      - run: pip install -r requirements.txt

      - name: Phase 5 — YouTube upload
        env:
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
        run: |
          if [ -z "$YOUTUBE_CLIENT_ID" ] || [ -z "$YOUTUBE_REFRESH_TOKEN" ]; then
            echo "YouTube secrets not set — video saved as artifact only."
            exit 0
          fi
          test -s output/final_video.mp4 || exit 1
          test -s output/thumbnail.png || exit 1
          python src/phase5_upload.py \
            --video output/final_video.mp4 \
            --metadata output/metadata.json \
            --seo output/youtube_seo.json \
            --captions output/captions.srt \
            --thumbnail output/thumbnail.png

      - name: Cleanup VPS run artifacts
        if: success()
        run: |
          pip install httpx -q
          python src/cleanup_vps.py \
            --input output \
            --run-id "${{ needs.script-and-audio.outputs.run_id }}" \
            --skip-movie

      - name: Alert on success
        if: success()
        env:
          ALERT_WEBHOOK_URL: ${{ secrets.ALERT_WEBHOOK_URL }}
        run: bash scripts/alert.sh "Retro recap complete — run ${{ needs.script-and-audio.outputs.run_id }}"

  notify-failure:
    needs: [script-and-audio, wait-for-render, upload]
    if: ${{ always() && (needs.script-and-audio.result == 'failure' || needs.wait-for-render.result == 'failure' || needs.upload.result == 'failure') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Alert on failure
        env:
          ALERT_WEBHOOK_URL: ${{ secrets.ALERT_WEBHOOK_URL }}
        run: bash scripts/alert.sh "Retro recap pipeline FAILED"
