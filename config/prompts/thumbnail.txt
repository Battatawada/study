Design ONE YouTube thumbnail for this tech explainer video.

Topic: {topic}
LOCKED video title: {title}
Thumbnail hook text (from hook package): {thumbnail_text}
Icon emoji (optional accent): {icon_emoji}

STYLE BRIEF (match Codist / Just Explained / Infinite Codes thumbnail patterns):
{style_notes}

We compose thumbnails programmatically (dark bg + bold text + emoji). Your job: specify text layout.

Reply with ONLY raw JSON:
{{
  "overlay_title": "2–4 words — topic shorthand (e.g. HTTP CODES, DATA STRUCTURES)",
  "overlay_subtitle": "EXPLAINED or IN 14 MIN",
  "time_badge": "14 MIN or 20 MIN — match video length",
  "text_color": "#FFFFFF",
  "accent_color": "#3B82F6",
  "bg_color": "#0f0f1a",
  "icon_emoji": "single emoji matching topic",
  "style_notes": "dark navy bg, bold white title, blue accent on subtitle chip, emoji top-right, clean sans-serif"
}}

Rules:
- High contrast, readable on mobile at 120px width
- Dark background (#0f0f1a or similar) — NOT white
- Title = topic name, NOT clickbait question
- Time badge in corner (e.g. "14 MIN") — matches competitor pattern
- No faces required — concept/icon driven
- No studio logos, no fake red arrows
